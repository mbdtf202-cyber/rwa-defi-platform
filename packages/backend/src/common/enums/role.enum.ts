// Role enum - matches Prisma schema
export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN',
  ISSUER = 'ISSUER',
  SPV_MANAGER = 'SPV_MANAGER',
  COMPLIANCE_OFFICER = 'COMPLIANCE_OFFICER',
  AUDITOR = 'AUDITOR',
}
